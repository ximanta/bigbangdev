import React, { createContext, useState, useContext } from 'react';const PlantContext = createContext();export const PlantProvider = ({ children }) => {const [plants, setPlants] = useState([]);const addPlant = (plant) => {setPlants((prevPlants) => [...prevPlants, { ...plant, id: Date.now().toString() }]);};const updatePlant = (updatedPlant) => {setPlants((prevPlants) => prevPlants.map((p) => (p.id === updatedPlant.id ? updatedPlant : p)));};const deletePlant = (plantId) => {setPlants((prevPlants) => prevPlants.filter((p) => p.id !== plantId));};return(<PlantContext.Provider value={{ plants, addPlant, updatePlant, deletePlant }}>{children}</PlantContext.Provider>);};export const usePlants = () => useContext(PlantContext);