import React from 'react';function InputField({ type = 'text', placeholder, value, onChange, label }){  return (    <div>      {label && (        <label          htmlFor={placeholder}          style={{ display: 'block', marginBottom: '5px' }}        >          {label}        </label>      )}      <input        type={type}        placeholder={placeholder}        value={value}        onChange={onChange}        className="input-field"        id={placeholder}      />    </div>  );}export default InputField;