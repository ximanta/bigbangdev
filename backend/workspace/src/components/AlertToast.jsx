import React, { useState, useEffect } from 'react';function AlertToast({ message, type = 'info', duration = 3000 }){  const [isVisible, setIsVisible] = useState(false);  useEffect(() => {    if (message) {      setIsVisible(true);      const timer = setTimeout(() => {        setIsVisible(false);      }, duration);      return () => clearTimeout(timer);    }  }, [message, duration]);  if (!isVisible) return null;  return (    <div className={`alert-toast ${type} ${isVisible ? 'show' : ''}`}>      {message}    </div>  );}export default AlertToast;