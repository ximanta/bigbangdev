import React, { useState } from 'react';import Button from './Button.jsx';const SearchBar = ({ onSearch, placeholder = 'Search plants...' }) => {const [query, setQuery] = useState('');const searchBarContainerStyle = {display:'flex',marginBottom:'20px'};const inputStyle = {flexGrow:1,padding:'10px',border:'1px solid #ccc',borderRadius:'4px 0 0 4px',fontSize:'1em'};const buttonStyle = {padding:'10px 15px',backgroundColor:'#4CAF50',color:'white',border:'none',borderRadius:'0 4px 4px 0',cursor:'pointer'};const handleSearch = () => {onSearch(query);};return(<div style={searchBarContainerStyle}><input type="text" value={query} onChange={(e) => setQuery(e.target.value)} placeholder={placeholder} style={inputStyle} onKeyPress={(e) => {if (e.key === 'Enter') handleSearch();}} /><Button onClick={handleSearch} style={buttonStyle}>Search</Button></div>);};export default SearchBar;