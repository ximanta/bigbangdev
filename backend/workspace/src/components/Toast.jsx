import React from 'react';const Toast = ({ message, type = 'info', onClose }) => {const baseStyle = {position:'fixed',bottom:'80px',left:'50%',transform:'translateX(-50%)',padding:'12px 20px',borderRadius:'8px',color:'white',fontSize:'0.9em',zIndex:2000,boxShadow:'0 4px 8px rgba(0,0,0,0.2)',display:'flex',alignItems:'center',minWidth:'200px',justifyContent:'space-between'};const infoStyle = {backgroundColor:'#2196F3'};const successStyle = {backgroundColor:'#4CAF50'};const errorStyle = {backgroundColor:'#F44336'};let specificStyle = infoStyle;if (type === 'success') specificStyle = successStyle;else if (type === 'error') specificStyle = errorStyle;const closeButtonStyle = {background:'none',border:'none',color:'white',fontSize:'1.2em',cursor:'pointer',marginLeft:'10px'};return(<div style={{...baseStyle,...specificStyle}}><span style={{flexGrow:1}}>{message}</span><button onClick={onClose} style={closeButtonStyle}>&times;</button></div>);};export default Toast;