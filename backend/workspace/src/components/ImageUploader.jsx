import React, { useRef, useState } from 'react';import Button from './Button.jsx';const ImageUploader = ({ onImageUpload, currentImage, style = {} }) => {const fileInputRef = useRef(null);const [previewUrl, setPreviewUrl] = useState(currentImage || null);const handleFileChange = (event) => {const file = event.target.files[0];if (file) {const reader = new FileReader();reader.onloadend = () => {setPreviewUrl(reader.result);onImageUpload(reader.result);};reader.readAsDataURL(file);}};const handleClick = () => {fileInputRef.current.click();};const containerStyle = {display:'flex',flexDirection:'column',alignItems:'center',marginBottom:'15px',...style};const imagePreviewStyle = {width:'100%',maxWidth:'200px',height:'150px',borderRadius:'8px',objectFit:'cover',marginBottom:'10px',border:'1px solid #eee'};const placeholderStyle = {width:'100%',maxWidth:'200px',height:'150px',borderRadius:'8px',backgroundColor:'#e0e0e0',display:'flex',alignItems:'center',justifyContent:'center',color:'#666',marginBottom:'10px',fontSize:'0.9em'};return(<div style={containerStyle}>{previewUrl ? (<img src={previewUrl} alt="Plant Preview" style={imagePreviewStyle} />) : (<div style={placeholderStyle}>No Image</div>)}<input type="file" accept="image/*" onChange={handleFileChange} ref={fileInputRef} style={{display:'none'}} /><Button onClick={handleClick} variant="secondary">Upload Image</Button></div>);};export default ImageUploader;