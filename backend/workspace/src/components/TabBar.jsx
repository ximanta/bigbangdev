import React from 'react';import { NavLink } from 'react-router-dom';const TabBar = ({ tabs, baseUrl }) => {const tabBarStyle = {display:'flex',justifyContent:'space-around',backgroundColor:'#f0f0f0',borderRadius:'8px',marginBottom:'16px',overflow:'hidden'};const tabLinkStyle = {flex:1,textAlign:'center',padding:'10px 0',textDecoration:'none',color:'#555',fontWeight:'bold',transition:'background-color 0.2s ease'};const activeTabLinkStyle = {...tabLinkStyle,backgroundColor:'#4CAF50',color:'white'};return(<div style={tabBarStyle}>{tabs.map((tab) => (<NavLink key={tab.path} to={`${baseUrl}/${tab.path}`} style={({ isActive }) => (isActive ? activeTabLinkStyle : tabLinkStyle)}>{tab.label}</NavLink>))}</div>);};export default TabBar;