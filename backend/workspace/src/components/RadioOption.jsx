import React from 'react';function RadioOption({ id, name, value, label, checked, onChange }){  return (    <label      htmlFor={id}      className={`radio-option ${checked ? 'selected' : ''}`}    >      <input        type="radio"        id={id}        name={name}        value={value}        checked={checked}        onChange={onChange}      />      <span className="radio-option-label">        {label}      </span>    </label>  );}export default RadioOption;